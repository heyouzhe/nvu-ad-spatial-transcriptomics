# Data Directory

Place processed local inputs here when running the figure notebooks. Data files
are intentionally ignored by Git.

Suggested subdirectories:

- `abeta/`: Aβ mask and plaque-distance tables.
- `figure7/`: GNN input tables, embeddings, LR summaries, and cached model data.
- `line/`: tissue boundary or line-coordinate files used for spatial plotting.
